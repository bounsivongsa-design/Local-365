## Packages
date-fns | Formatting dates for events and posts
framer-motion | Smooth animations for page transitions and interactions
lucide-react | Icon set (already in base, but emphasizing usage)
react-hook-form | Form handling
zod | Schema validation
@hookform/resolvers | Zod integration for react-hook-form

## Notes
- Auth is handled via Replit Auth (useAuth hook).
- Images use Unsplash URLs as placeholders.
- Tailwind config needs to extend font family to support Google Fonts defined in index.css.
